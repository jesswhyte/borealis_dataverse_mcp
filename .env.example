# Borealis MCP Server Environment Variables

# Authentication tokens for HTTP mode (comma-separated)
# Generate secure random tokens for production
AUTH_TOKENS=your-secret-token-1,your-secret-token-2

# Optional: Borealis API key for accessing unpublished datasets
# Get from https://borealisdata.ca
BOREALIS_API_KEY=your-borealis-api-key-here

# Transport mode (auto-detected by default)
# Options: auto, stdio, http
# MCP_TRANSPORT=auto

# HTTP server configuration (for Railway or local HTTP testing)
# PORT=8000
# HOST=0.0.0.0
